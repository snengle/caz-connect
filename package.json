{
  "name": "caz-connect",
  "version": "1.0.0",
  "description": "Caz Connect: A Game of Gravity & Connection",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npm run build && npx serve ."
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "serve": "^14.2.1",
    "typescript": "^5.4.5"
  }
}
